//! Работа с байтовыми буферами.
//!
//! Ядро парсит недоверенный вход, поэтому прямая индексация срезов запрещена
//! линтом `clippy::indexing_slicing` на уровне workspace. [`Reader`] — **единственное
//! место, где срез вообще нарезается**: любая проверка границ сосредоточена здесь,
//! и её достаточно проверить один раз, вместо того чтобы искать пропущенный
//! `if` в тысячах строк парсеров.

mod reader;
mod span;
mod writer;

pub use reader::Reader;
pub use span::Span;
pub use writer::{Patch, Writer};
